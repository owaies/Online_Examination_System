export const metadata = {
  title: "Teacher Dashboard | E-Examiner",
  robots: {
    index: false,
    follow: false,
    nocache: true
  }
};

export default function TeacherLayout({ children }) {
  return children;
}
