export const metadata = {
  title: "Exam Session | E-Examiner",
  robots: {
    index: false,
    follow: false,
    nocache: true
  }
};

export default function QuizSessionLayout({ children }) {
  return children;
}
